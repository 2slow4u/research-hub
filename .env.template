# ResearchHub Environment Configuration Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env files to version control!

# ================================================================
# DATABASE CONFIGURATION
# ================================================================
DATABASE_URL=postgresql://username:password@host:port/database
PGHOST=your-postgresql-host
PGPORT=5432
PGUSER=your-database-user
PGPASSWORD=your-database-password
PGDATABASE=your-database-name

# ================================================================
# AUTHENTICATION & SESSION
# ================================================================
# Generate a secure random string for SESSION_SECRET (32+ characters)
SESSION_SECRET=your-very-long-secure-session-secret-change-this-in-production

# Your Replit app ID for authentication
REPL_ID=your-replit-app-id

# OpenID Connect issuer URL (usually don't change this)
ISSUER_URL=https://replit.com/oidc

# Your Replit domain(s) - comma separated if multiple
REPLIT_DOMAINS=your-app-name.your-username.replit.app

# ================================================================
# TELEGRAM BOT INTEGRATION (OPTIONAL)
# ================================================================
# Get this from @BotFather on Telegram
# IMPORTANT: Keep this secret! Never commit to git!
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-from-botfather

# ================================================================
# AI PROVIDER API KEYS (OPTIONAL)
# ================================================================
# These can also be configured through the app's Settings → AI Models
# It's recommended to use the in-app configuration for better security

# OpenAI API Key
OPENAI_API_KEY=your-openai-api-key

# Anthropic API Key
ANTHROPIC_API_KEY=your-anthropic-api-key

# Google AI (Gemini) API Key
GEMINI_API_KEY=your-gemini-api-key

# Azure OpenAI (if using Azure instead of OpenAI directly)
AZURE_OPENAI_API_KEY=your-azure-openai-key
AZURE_OPENAI_ENDPOINT=your-azure-endpoint

# ================================================================
# DEVELOPMENT ENVIRONMENT
# ================================================================
NODE_ENV=development

# ================================================================
# SECURITY NOTES
# ================================================================
# 1. NEVER commit this file to version control
# 2. Use strong, unique passwords and secrets
# 3. Regenerate any tokens that may have been exposed
# 4. Use Replit Secrets for production deployment
# 5. Monitor your API usage for unexpected activity
# 6. Keep your dependencies updated for security patches